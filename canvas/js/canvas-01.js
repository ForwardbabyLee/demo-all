(function(){
    // console.log(0)
    var defaults = {
        nodeID: '',
        callback: function(){},
        strokes: '',
        color: ''
    };

    var PlugCode = function(options){
        var settings = Object.assign({} ,defaults,options);
    }
})();
